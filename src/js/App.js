import React, { Component } from 'react';
import Home from './containers/pages/Home';

class App extends Component {
    // TODO! react-router will goes here once connection with Map back-end will be ready
  render() {
        return (
            <Home />
        );
  }
}

export default App;
